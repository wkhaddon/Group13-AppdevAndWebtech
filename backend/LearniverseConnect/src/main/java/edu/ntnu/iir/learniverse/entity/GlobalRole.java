package edu.ntnu.iir.learniverse.entity;

/**
 * GlobalRole is an enum representing the different roles a user can have in the system.
 */
public enum GlobalRole {
  USER,
  PROVIDER,
  SUPPORT,
  ADMIN
}
